apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  namespace: main
  name: scalingo-ingress
  labels:
    app: ingress-app
spec:
  rules:
    - http:
        paths:
          - path: /
            backend:
              serviceName: scalingo-service
              servicePort: 3000